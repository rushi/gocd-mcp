# GoCD Server URL (include port if non-standard)
GOCD_SERVER_URL=https://gocd.example.com:8154

# GoCD API Token (Bearer token authentication)
# Generate from GoCD: User Menu > Personal Access Tokens
GOCD_API_TOKEN=your-api-token-here

# Log level for pino logger (optional, defaults to 'info')
# Options: trace, debug, info, warn, error, fatal
LOG_LEVEL=info
